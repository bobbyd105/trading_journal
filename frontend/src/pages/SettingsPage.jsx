function SettingsPage() {
  return (
    <section id="settings" className="panel">
      <p className="eyebrow">Local-first</p>
      <h2>Settings</h2>
      <p>Settings will focus on local account configuration and SQLite database location.</p>
    </section>
  );
}

export default SettingsPage;
